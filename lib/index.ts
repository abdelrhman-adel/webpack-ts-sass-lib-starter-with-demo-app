/**
 * import your styles here, if you have multiple entry points
 */
import "./style";

/**
 * bootstrap your library here and export it
 */
(text => console.log(text))(
  "just a test function, you can see it in your browser log"
);
